/// <reference types="node" />
import { Readable } from 'stream';
import { SQLQuery } from '@databases/sql';
import AbortSignal from '../types/AbortSignal';
import PgClient from '../types/PgClient';
export declare function queryNodeStream(client: PgClient, query: SQLQuery, options: {
    highWaterMark?: number;
}): Readable;
export declare function queryStream(client: PgClient, query: SQLQuery, { batchSize, signal }: {
    batchSize?: number;
    signal?: AbortSignal;
}): AsyncGenerator<any, void, unknown>;
